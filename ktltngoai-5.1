#include <iostream>
#include <math.h>
#include <conio.h>
#include <string.h>
using namespace std;
int main (){
    int arr[1000], n, k,t =0, sum =0;
    cout <<"Nhap so phan tu cua mang arr : ";
    cin >>n;
    cin.clear();
    for (int i = 1; i<=n; i++)
    {
        cout << "phan tu thu "<<i<<" = ";
        cin >> arr[i];
    }
    for (int i = 1; i<=n; i++)
    {
     for (int j = i; j <=n-1; j++)
     {
         if (arr[i] < arr[j])
         {
             k = arr[i];
             arr[i]= arr[j];
             arr[j] = k;
         }
     }  
        if (arr[i] %2 != 0)
        {
            t++;
            sum += arr[i];
        }
            }
    cout <<"mang duoc sap xep lai la : ";
    for (int i = 1; i<=n; i++)
    {cout<< arr[i]<< " ";}
    cout <<endl<< "so phan tu le trong mang la : "<<t <<" ; Co tong = "<<sum;
}
